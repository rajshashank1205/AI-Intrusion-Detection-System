import { colors } from "./colors";

export const theme = {
  radius: {
    sm: "8px",
    md: "12px",
    lg: "16px",
    xl: "24px",
  },

  shadow: {
    card: "0 10px 30px rgba(0,0,0,.25)",
  },

  colors,
};